import { useState } from "react";
import { Activity, ArrowRight, Building2, ShieldCheck, Smartphone, Users } from "lucide-react";
import { useLocation, useNavigate } from "react-router-dom";
import { isValidMobile, setStoredMobile, setStoredRole, type AppRole } from "@/lib/auth";

const roleOptions: Array<{ role: AppRole; title: string; description: string; destination: string; icon: typeof Users; accent: string }> = [
  { role: "admin", title: "Admin / authority", description: "Command center, incident intelligence, resource allocation, and response analytics.", destination: "/command-center", icon: Building2, accent: "#e55252" },
  { role: "ordinary", title: "Ordinary user", description: "Public disaster map, risk analysis, alerts, shelters, hospitals, and incident reporting.", destination: "/", icon: Users, accent: "#337e69" },
];

export default function SignIn() {
  const navigate = useNavigate();
  const location = useLocation();
  const [selected, setSelected] = useState<AppRole>("ordinary");
  const [mobile, setMobile] = useState("");
  const [error, setError] = useState("");
  const from = (location.state as { from?: string } | null)?.from;
  const selectedOption = roleOptions.find((option) => option.role === selected) ?? roleOptions[1];

  const enterPlatform = () => {
    if (selected === "ordinary" && !isValidMobile(mobile)) {
      setError("Enter a valid mobile number with country code, for example +919876543210.");
      return;
    }
    setError("");
    setStoredRole(selected);
    if (selected === "ordinary") setStoredMobile(mobile.trim());
    navigate(selected === "admin" ? "/command-center" : from && !from.startsWith("/command-center") && !from.startsWith("/analytics") && !from.startsWith("/resources") ? from : "/", { replace: true });
  };

  return <main className="relative flex min-h-screen items-center justify-center overflow-hidden bg-[#f7f9fb] px-5 py-12"><div className="grid-pattern absolute inset-0 opacity-70" /><div className="relative grid w-full max-w-[1000px] overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-[0_25px_80px_rgba(35,58,62,.12)] lg:grid-cols-[.9fr_1.1fr]"><section className="relative overflow-hidden bg-[#12232a] px-7 py-9 text-white sm:px-10 sm:py-12"><div className="absolute -right-20 -top-20 h-56 w-56 rounded-full border border-[#75c9a6]/20" /><div className="absolute -bottom-32 -left-20 h-72 w-72 rounded-full border border-[#e55252]/20" /><div className="relative"><div className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#e55252] text-white shadow-[0_8px_22px_rgba(229,82,82,.24)]"><Activity size={20} strokeWidth={2.5} /></span><div><p className="text-[16px] font-extrabold tracking-tight">Disaster<span className="text-[#ef7470]">AI</span></p><p className="text-[9px] font-semibold uppercase tracking-[.2em] text-slate-400">Civic intelligence</p></div></div><div className="mt-20 max-w-[380px]"><p className="flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-[.18em] text-[#75c9a6]"><ShieldCheck size={14} /> Secure demo access</p><h1 className="mt-4 text-[38px] font-extrabold leading-[1.05] tracking-[-.05em] sm:text-[48px]">Know more.<br /><span className="text-[#ef7470]">Respond faster.</span></h1><p className="mt-5 text-[14px] leading-6 text-slate-400">One operating picture for India’s disaster awareness, emergency coordination, and public safety.</p></div><div className="mt-20 flex items-center gap-2 text-[10px] font-semibold text-slate-500"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-[#75c9a6]" /> Prototype environment · illustrative data</div></div></section><section className="px-6 py-8 sm:px-10 sm:py-12"><div className="mx-auto max-w-[430px]"><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-[#337e69]">Welcome to DisasterAI</p><h2 className="mt-3 text-[28px] font-extrabold tracking-[-.045em] text-slate-950">Choose your workspace</h2><p className="mt-3 text-[13px] leading-6 text-slate-500">Select how you want to enter the platform. You can return to this screen anytime to choose a different demo role.</p>{selected === "ordinary" && <label className="mt-6 block text-[10px] font-extrabold uppercase tracking-[.14em] text-slate-500"><span className="flex items-center gap-2"><Smartphone size={13} className="text-[#337e69]" /> Mobile number for alerts</span><input value={mobile} onChange={(event) => { setMobile(event.target.value); setError(""); }} inputMode="tel" placeholder="+919876543210" className="mt-2 h-11 w-full rounded-lg border border-slate-200 px-3 text-[12px] font-semibold normal-case tracking-normal text-slate-700 outline-none placeholder:text-slate-400 focus:border-[#337e69]" /><span className="mt-1.5 block text-[9px] font-medium normal-case tracking-normal text-slate-400">Used for disaster-alert subscriptions and SMS replies such as HELP, SAVE, or RESCUE.</span></label>}{error && <p className="mt-3 rounded-lg bg-[#fff3f1] p-3 text-[10px] leading-4 text-[#c34f4e]">{error}</p>}<div className="mt-8 space-y-3">{roleOptions.map((option) => { const Icon = option.icon; const active = option.role === selected; return <button key={option.role} onClick={() => setSelected(option.role)} className={`flex w-full items-start gap-4 rounded-xl border p-4 text-left transition ${active ? "border-[#b8d8ca] bg-[#f3f9f6] shadow-sm" : "border-slate-200 bg-white hover:border-slate-300"}`}><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg" style={{ color: option.accent, backgroundColor: `${option.accent}18` }}><Icon size={19} /></span><span className="flex-1"><span className="flex items-center justify-between gap-2"><span className="text-[13px] font-extrabold text-slate-900">{option.title}</span><span className={`h-4 w-4 rounded-full border-2 ${active ? "border-[#337e69] bg-[#337e69]" : "border-slate-300"}`} /></span><span className="mt-1.5 block text-[11px] leading-5 text-slate-500">{option.description}</span></span></button>; })}</div><button onClick={enterPlatform} className="mt-7 flex h-12 w-full items-center justify-center gap-2 rounded-lg bg-[#e55252] text-[12px] font-extrabold text-white shadow-[0_10px_20px_rgba(229,82,82,.18)] transition hover:bg-[#d94a4d]">Continue as {selectedOption.role === "admin" ? "admin" : "ordinary user"} <ArrowRight size={15} /></button><p className="mt-4 flex items-center justify-center gap-2 text-center text-[10px] leading-4 text-slate-400"><ShieldCheck size={13} className="text-[#337e69]" /> Demo role selection · real authentication can be connected later</p></div></section></div></main>;
}
